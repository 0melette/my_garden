# Local working data

`almanac.db` is the live database and is ignored by Git. The `plant_images` directory is the local image store.

Use the export script in the repository root when you want a reviewable catalogue version in Git.
